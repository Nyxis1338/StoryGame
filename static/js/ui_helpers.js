const UIHelpers = {
    // 显示 Toast 消息（由外部传入容器）
    showToast(message, type = 'success', duration = 2000) {
        // 具体实现可复用之前 creator.js 中的逻辑
    },
    // 显示确认对话框（返回 Promise）
    showConfirm(title, message, confirmText, cancelText, isDanger) {
        // 返回 Promise，通过模态框交互
    }
};